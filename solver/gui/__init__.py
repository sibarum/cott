"""
GUI package for the COTT Calculator.

Contains the main calculator app, fullscreen viewer, and settings window.

Submodules:
    constants   — Color palette and layout constants
    utils       — Tick formatting, color scales, line clipping
    app         — CalculatorApp (main window)
    fullscreen  — FullScreenViewer (pan/zoom viewer)
    settings    — SettingsWindow (configuration dialog)
"""
